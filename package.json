{
  "name": "form-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node esbuild.config.js",
    "build:dev": "esbuild assets/js/main.js --bundle --outfile=assets/js/bundle.js --format=esm",
    "build:parks": "esbuild assets/js/main.js --bundle --outfile=assets/js/parks-bundle.js --format=esm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.25.1"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  }
}
